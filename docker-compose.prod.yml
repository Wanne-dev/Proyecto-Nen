version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
  ia-service:
    build: ./ia-service
    ports:
      - "8000:8000"
